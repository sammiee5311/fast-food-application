input {
  kafka {
    client_id => "logstash-registered-user"
    group_id => "logstash-registered-user"
    topics => ["registered-user"]
    bootstrap_servers => "kafka:29092"
  }
}

output {
        elasticsearch {
                hosts => "elasticsearch:9200"
                index => "registered-user"
        }
}